{
  "name": "micropress-theme-liquid-glass",
  "version": "0.0.10",
  "description": "A custom MicroPress theme",
  "main": "dist/renderers.js",
  "types": "dist/renderers.d.ts",
  "scripts": {
    "build": "micropress-theme build",
    "validate": "micropress-theme validate",
    "package": "micropress-theme package",
    "version": "node -e \"const m=require('./manifest.json'); m.version=require('./package.json').version; require('fs').writeFileSync('manifest.json', JSON.stringify(m, null, 2)+'\\n')\" && git add manifest.json"
  },
  "devDependencies": {
    "@micropress/theme-sdk": "^0.1.1",
    "typescript": "^5.0.0"
  },
  "keywords": [
    "micropress",
    "theme"
  ],
  "license": "MIT"
}
